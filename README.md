# fintual-se-task

There are two ways of running this project:
- `npm run start` will run what is on index.ts, where the UserStocks and allocations are defined in the code
- `npm run cli` will prompt the user on the terminal to input the values for each stock (name, quantity, price history) and the allocation for each stock.